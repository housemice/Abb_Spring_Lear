# Database connection properties
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_manager
    username: root
    password: daniilkuz133
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Hibernate properties
  jpa:
    hibernate:
      ddl-auto: update
